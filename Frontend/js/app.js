class App {
    constructor() {
        // Инициализация приложения
        this.init();
    }

    init() {
        console.log("Приложение инициализировано");
        // Здесь можно добавить логику для загрузки начальных данных или настроек
    }

    navigateTo(page) {
        // Логика для глобальной навигации между страницами
        // page — название страницы, на которую нужно перейти
        console.log(`Переход на страницу: ${page}`);
    }

    handleError(error) {
        // Обработка ошибок
        console.error("Произошла ошибка:", error);
        // Здесь можно добавить вывод сообщения об ошибке на интерфейс
    }
}

const app = new App();
